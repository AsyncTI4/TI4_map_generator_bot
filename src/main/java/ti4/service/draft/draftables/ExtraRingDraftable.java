package ti4.service.draft.draftables;

// e.g. add a 4th ring to a standard 3-ring map, with the outer ring tiles being separately draftable.
// Generate 4-tile "slices" which go *behind* your home system.
// Draft them to add to the board.

public class ExtraRingDraftable {}
